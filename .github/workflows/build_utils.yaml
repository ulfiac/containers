name: build utils

on:
  workflow_dispatch:
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/build_utils.yaml
      - utils/**
  push:
    branches:
      - main
    paths:
      - .github/workflows/build_utils.yaml
      - utils/**

permissions:
  contents: read

jobs:
  build-image:
    name: ${{ github.event_name == 'pull_request' && 'build image as a PR check' || 'build image and push' }}
    runs-on: ubuntu-24.04-arm
    permissions:
      contents: read      # checkout
      packages: write     # push image to GHCR
    steps:
      - name: checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: set build timestamp
        id: timestamp
        run: echo "value=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> "$GITHUB_OUTPUT"

      - name: build and load image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: utils
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.timestamp.outputs.value }}
          load: true
          platforms: linux/arm64
          push: false
          tags: |
            ghcr.io/ulfiac/utils:latest
            ghcr.io/ulfiac/utils:${{ github.sha }}

      - name: scan image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          exit-code: '0'
          image-ref: 'ghcr.io/ulfiac/utils:${{ github.sha }}'

      # pull request checks stop here; push to main keeps going

      - name: login to ghcr.io
        if: github.event_name != 'pull_request'
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: push image
        if: github.event_name != 'pull_request'
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: utils
          labels: |
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.created=${{ steps.timestamp.outputs.value }}
          platforms: linux/arm64
          push: true
          tags: |
            ghcr.io/ulfiac/utils:latest
            ghcr.io/ulfiac/utils:${{ github.sha }}
