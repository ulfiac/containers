name: _purge_workflow_runs

on:
  schedule:
    - cron: '53 8 * * *'  # daily, 08:53 UTC (03:53 EST, 04:53 EDT)
  workflow_dispatch:

permissions:
  actions: write
  contents: read
  packages: write

jobs:
  purge-workflow-runs:
    uses: ulfiac/commons/.github/workflows/reusable_purge_workflow_runs.yaml@e5b4cc336691ac31fdf36038a053d5ed0a8f9e5f # v1.0.40
