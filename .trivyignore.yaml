misconfigurations:
  - id: AVD-DS-0026
    # DS-0026 (LOW): Add HEALTHCHECK instruction in your Dockerfile
    # You should add HEALTHCHECK instruction in your docker container images to perform the health check on running containers.
    paths:
      - terraform-fmt/Dockerfile
      - terragrunt-hcl-fmt/Dockerfile
      - tflint/Dockerfile
      - utils/Dockerfile
    statement: These are CLI/tool containers, not long-running services; Docker HEALTHCHECK is not meaningful for their runtime model.
